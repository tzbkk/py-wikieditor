# convert_cat_page.md

此文档已删除，因为该脚本已不再存在。

此脚本的功能已整合到 `convert_category.py` 的 `--page` 选项中。

使用方法：
```bash
python src/convert_category.py --page "片頭曲"
```

有关更多信息，请参阅 [convert_category.py](./convert_category.md) 或运行 `python src/fandom.py category --help`。
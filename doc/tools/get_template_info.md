# get_template_info.md

此文档已删除，因为该脚本已不再存在。

此脚本的功能已整合到 `fandom.py` 的 `info` 命令中。

使用方法：
```bash
python src/fandom.py info "Template:模板名"
```

有关更多信息，请参阅 [fandom.py](./fandom.py) 或运行 `python src/fandom.py info --help`。
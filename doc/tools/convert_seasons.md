# convert_seasons.md

此文档已删除，因为该脚本已不再存在。

此脚本的功能已整合到 `convert_page.py` 的 `--with-subpages` 选项中。

使用方法：
```bash
python src/convert_page.py "动画第一季" --with-subpages
```

有关更多信息，请参阅 [convert_page.py](./convert_page.md) 或运行 `python src/fandom.py page --help`。